import { Text, View } from 'react-native';
import { mainHeaderStyles } from '../styles/mainHeader';

export function BuyerHome() {
  return (
    <View style={mainHeaderStyles.headerContainer}>
      <Text style={mainHeaderStyles.headerTitle}>Hello</Text>
    </View>
  );
}
